<?php

/**
 * Assignment Operators.
 *
 * =, +=, -=, /=, *=, %=, **=, &=, |=, ^=, <<=, >>=, .=
 */

 $message = 'Hello, ';
 echo $message; // -> Hello, 
 echo "\n"; // -> Hello, 
 $message .= 'world'; // 문자열 붙이기

 echo $message; // -> Hello, world